# Fork Patch Registry

该文件只维护尚未进入上游、但需要保留在个人版本中的改动。

当前没有个人补丁。

新增条目时使用以下模板：

```markdown
## 功能名称

- 状态：`upstream-review` / `fork-only` / `rejected` / `superseded`
- 分支：`personal/example`
- 关键提交：`<commit>`
- 上游 Issue/PR：`Aliothmoon/MAA-Meow#<number>`
- 保留原因：
- 影响路径：
- 兼容性风险：
- 验证记录：
- 最近同步：`YYYY-MM-DD / <upstream-commit>`
```

